# DECISIONS — ZAYA 45 s "6D" TikTok motion graphic

Autonomous production run, 2026-09-06. Every place where the brief was silent, ambiguous, or in
tension with the repository's binding honesty rules (`CLAUDE.md` F4) is recorded here with the
decision taken and why. Nothing below was cleared with the founder — it is the log for that review.

## 0. Scope & pipeline
| # | Decision | Why |
|---|---|---|
| 0.1 | Built as a **self-contained render pipeline** in `motion/tiktok-6d/` with its own `package.json` (three@0.185.1, gsap@3.15.0). Nothing is imported into the Astro site; zero added JS on the website. | `CLAUDE.md`: heavy libs never ship on the site. The film is an offline render, not a page. |
| 0.2 | Primary stack from §9 used as specified: Three.js WebGL2 + one paused GSAP master timeline, driven by `renderFrame(t, dt)` (never wall-clock), captured frame-by-frame with the pre-installed Playwright Chromium (SwiftShader, `PLAYWRIGHT_BROWSERS_PATH=/opt/pw-browsers`, no `playwright install`), encoded with ffmpeg. No fallback in the §9 chain was needed. | Deterministic, reproducible, re-renderable. |
| 0.3 | ffmpeg: the Playwright-bundled ffmpeg has no libx264, and `apt-get install ffmpeg` 404'd on this image's mirror; used the static ffmpeg 7.0.2 shipped in the `imageio-ffmpeg` PyPI wheel (`pip3 install imageio-ffmpeg`), which has libx264. Path is read from `$FFMPEG` in `encode.mjs`. | Spec needs H.264 High / CRF 18 / +faststart. |
| 0.4 | Render outputs (`out/`, `deliverables/`) are **git-ignored**; only source + docs are committed. The MP4/PNG deliverables are handed over as files. | 100 MB video does not belong in the website repo. |

## 1. Context pulled from the repo (what the film is built from)
- Palette: **official tokens** in `src/styles/tokens.css` / `docs/06-brand-and-design-tokens.md` — teal `#0EA5A4` (bright `#13B7B4`, deep `#0B8B8A`), coral `#FF7A45`, navy `#1E2A4A`, amber `#F4A261`, plum `#6D5DD3` (jacaranda-hour light, sparingly), mint `#5DCAA5`, cloud `#F4F7F8`. Ground `#0A0F12` as the brief asked.
- Logo: pin path, coral disc and Z polyline extruded/tubed **from the actual SVG** (`brand/2026-07-07_ZAYA_Logo_AppIcon.svg`); wordmark strokes from `brand/…_Logo_Wordmark.svg` (on-dark white variant, "APP" in coral, as the app header does).
- Type: **Poppins** (400–800) and **Noto Sans Ethiopic** (400–700) from `src/assets/fonts/*.woff2`, loaded with the FontFace API — bundled, no CDN.
- App design ("the recently designed ZAYA app"): the live-drawn UI panels copy the **real app captures** in `src/assets/demo/*.png` (navy header, `ZAYA.` teal wordmark with coral dot, cloud surface, "Welcome / Everything near you.", Scan · Browse near · My orders, "Shops near you" with the seeded Amharic shop names, the merchant Dashboard tiles and "Today's sales ETB 872.00 / Products 8", the order tracker's "Waiting for the shop" and "Cash on delivery" copy, "ETB 122.00" order). Numbers shown are the values in those real captures — nothing invented.
- Icons: **Customers** = the site's "Shops in your area" magnifier; **Diaspora** = the site's "Provide for family" basket (both from `src/pages/index.astro`). The site has **no Merchant or Ride glyph**, so a storefront and a route icon were designed in the same 24-grid Tabler stroke grammar. All four are true 3D (stroke paths → tube geometry with round caps, PBR clearcoat).

## 2. Honesty rules applied (F4) — where the brief and the repo disagreed, the repo won
| # | Brief asked | Shipped | Ground |
|---|---|---|---|
| 2.1 | Segments **Ride** and **Diaspora** as products. | Both present, each carrying a persistent **"Planned · የታቀደ"** pill (the site's own labelling axis) for the whole segment; "Delivery" is the segment label (the brief's "Ride" name is kept for the icon/beat). No rider, vehicle or ZAYA ride product is shown — the marker on the route is the **package**, handed to the customer at her door (delivery is shop-managed at pilot). | `CLAUDE.md`: unbuilt verticals appear only as clearly labelled roadmap; "there is no Rider ZAYA product". |
| 2.2 | Caption "The shop is **200 m** away"; a "distance ring"; ETA counter ticking. | "The shop is right there. / You still can't find it." — no metre values anywhere; the ring motif is the brand's own zone-ring radar, not a measured radius; the order tracker advances **status** (Waiting → Confirmed → On its way → Delivered) instead of an invented ETA. Shops are labelled by **area** (Jemo, Nifas Silk, Haile Garment) exactly as the real app does. | The website's zero-tolerance proximity ratchet (`scripts/test-claims.mjs`); the platform has no shop lat/lng. |
| 2.3 | **Telebirr / M-Pesa / Chapa** payment chips. | Replaced by chips for what is actually built: **Cash on delivery**, **Credit book**, **Quick sale**. | No partner logos or payment-unification claims we do not have (`index.astro` comment, acceptance checklist "no partner logos we don't have"). Third-party marks never used as ours. |
| 2.4 | "Your shop, online **in minutes**." | "Your shop. Online." | Onboarding time is an unapproved claim. |
| 2.5 | App-store row. | Two neutral pills "Android — Coming soon" / "iOS — Coming soon"; **no Apple/Google badges**. | Pre-launch; no store listing exists; no third-party marks. |
| 2.6 | Count-ups / metrics. | Only the real capture values (ETB 872.00, 8 products, 1 order waiting, ETB 122.00). No users, ratings, testimonials, merchants counts. | No invented metrics. |
| 2.7 | Domain / URL on the end card. | None. | Domain not settled (`docs/13-flagship-domain-zaya-et.md`); site is `published:false`. |

## 3. Humans (§5) — sourcing path taken
- Tier 1 ("real photography already in the repo"): the repo holds exactly two founder-cleared persona images, both **AI-generated by the founder** and restored by ruling on the condition that they carry a visible **"Illustration · ምሳሌያዊ ምስል"** label (`src/assets/photos/RIGHTS.md`). They are used for **Merchants** (`credit-book.jpg`) and **Diaspora** (`diaspora-family.jpg`), as graded, feathered parallax cards with a rim light, and the chip is burned in next to each one. Prep (`assets/person-*.png`, made by the PIL script recorded in the git history of this folder): the merchant crop **excludes the counter card with gibberish lettering** and the notebook; the far shelves are defocused so no label reads as text; the family crop turns the grocery-box labels and the wall portrait into foreground/background bokeh. Hands: the merchant's phone-holding hand is intact (five fingers, natural grip); the family frame shows no problem hands.
- No cleared **customer** image exists (the founder left `customer-app.jpg` out for trademark + fabricated-UI reasons), and no image-generation tool was connected. Per the brief's fallback that "never looks cheap", the customer is an honest **stylised rim-lit figure** (layered canvases: braids with sway, hoodie with follow-through, a real-looking Android phone that lights her face teal, breathing, micro head-turns, a blink), integrated in the 3D scene with a contact shadow. She is deliberately not a synthetic face.
- Consequence to flag for the founder: the two photos are the same AI images the truth pass removed from the *site* and later restored *only with the label*; they are used here under that same condition. If the ruling does not extend to video, swap them for commissioned pilot-shop photos (slots and direction in `docs/10-hero-imagery-brief.md`) — see README "re-render".

## 4. 6D / technical budget (measured, SwiftShader on 4 vCPU)
- Full-res 1080×1920 frame cost with the spec's chain as written (scene.environment PBR everywhere, transmission glass, SSAO, BokehPass): **≈ 9–12 s/frame** → 2700 frames ≈ 8 h. Rebuilt the post chain to a custom pipeline (`scene/pipeline.js`) that keeps every listed stage but budgets it: env-mapped PBR only on hero objects; **grab-pass screen-space refraction** for glass instead of `transmission`; **AO baked** into vertex colour + contact-shadow blobs instead of SSAO; DOF from half+quarter-res blurs with a depth CoC; **reprojection motion blur** (previous-frame view-projection, 8 taps); bloom → chromatic aberration + barrel distortion → ACES → sRGB → grain (3.5 %) → vignette. Result **≈ 3.2 s/frame** → 60 fps master ≈ 2.4 h. SwiftShader already uses all cores, so parallel workers do not help (measured: 4 workers ≈ 4× slower each).
- Frame rate: the 60 fps master was started (2700 frames ≈ 2.4 h on this box) and, at the founder's request to deliver sooner, finished as the spec's sanctioned **30 fps fallback**: the 60 fps frames already rendered were reused (every second frame is bit-identical to the 30 fps frame, the timeline is time-based) and only the remaining 30 fps frames were rendered. `node render.mjs --fps 60` re-renders the 60 fps master unchanged in ≈ 2.4 h.
- Everything is deterministic: seeded RNG (`util.rng`), value-noise handheld, GSAP timeline seeked by time, canvas UIs redrawn from tweened state.

## 5. Creative calls
- One unbroken camera journey; segment changes are morphs: phone → glass panel; panel **folds** (edge-on texture swap) into the merchant dashboard; shelf blocks **fold down into the road** and become the route's waypoints; the route's end **keeps travelling** as the globe arc; globe **collapses into the mark**; four icons converge into the diamond.
- Music: the brief said silent; the founder asked for Ethiopian-flavoured music. Shipped an **original synthesised score** (`music.py`, Tizita pentatonic, krar/kebero timbres) rather than any licensed or stock track. Founder may replace it with a produced track using AUDIO-BRIEF.md.
- Beat grid 120 BPM: light pulse on rims/glass edges every 0.5 s; hits (streak impact 0.66 s, ignition 7.0 s, icon lands, chip locks, arc landing 35.5 s, lock-up 41.2 s) sit on the grid — see `AUDIO-BRIEF.md`.
- Amharic secondary lines are machine-authored (same status as the app's own `am` strings, which the app marks DRAFT pending native review). Spellings used are standard: ሁሉም ነገር በአቅራቢያዎ · በአቅራቢያዎ ያግኙት · ሱቅዎ በመስመር ላይ · ታዘዘ። እየመጣ ነው። ደረሰ። · ወደ ቤት ይላኩ · ለኢትዮጵያ የተሰራ። ለእርስዎ የተሰራ። · በቅርቡ ይመጣል · የታቀደ · ምሳሌያዊ ምስል; shop signs ሱቅ · እንጀራ ቤት · ካፌ · ፋርማሲ · ሚኒ ማርኬት. **Flagged for the same native-speaker pass before publishing.**
- Globe: a data-globe (graticule + dotted land bands + labelled anchors) rather than a third-party map texture — nothing to license, and it stays on-brand. Anchor cities: Washington DC → Addis Ababa.
- Blue-and-white minibuses, corrugated-style low shopfronts with Amharic signs, jacaranda-violet fill, habesha cross-hatch weave as a faint procedural ground texture (never clip-art).
