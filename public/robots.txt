# F4 BINDING carve-out: the site must NOT be indexed until real Privacy + Terms
# and real content exist (site.published === true). Until then, block all crawlers.
# When published, a build step regenerates this to allow indexing + link the sitemap.
User-agent: *
Disallow: /

# (Post-publish this becomes:)
# User-agent: *
# Allow: /
# Sitemap: https://zayaethiopia.com/sitemap-index.xml
