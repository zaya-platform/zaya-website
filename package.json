{
  "name": "zaya-website",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "ZAYA Digital Front Door — public marketing website + navigational gateway. Standalone; no backend, no auth, no payments. One backend, many front doors.",
  "scripts": {
    "dev": "astro dev",
    "build": "node scripts/check-publish.mjs && astro build",
    "build:draft": "node scripts/check-draft.mjs && astro build",
    "preview": "astro preview",
    "check": "astro check",
    "test:assistant": "node scripts/test-assistant.mjs",
    "test:interactions": "node scripts/test-interactions.mjs",
    "test:gate": "node scripts/test-assistant-gate.mjs",
    "test:oauth": "node scripts/test-cms-oauth.mjs",
    "test": "node scripts/test-assistant-gate.mjs && node scripts/test-assistant.mjs && node scripts/test-interactions.mjs && node scripts/test-cms-oauth.mjs"
  },
  "dependencies": {
    "astro": "^4.15.0",
    "@astrojs/sitemap": "^3.2.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
